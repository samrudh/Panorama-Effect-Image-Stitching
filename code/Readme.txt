Intstruction to run the code:

1. Open constants.py 
2. Add the path of images in image_list variable.

If using Windows machine write path with r"<-path-to-image>" to avoid parsing issues

3. For certain test images, we might want to change the distance thresholds available in dist_thresh varaiable in application.py at line 198